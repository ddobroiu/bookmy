# Example environment variables
NEXT_PUBLIC_APP_NAME=Bookmy
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/bookmy
# Production example (fill with your provider URL or add as .env.production):
# DATABASE_URL=postgresql://user:password@host:5432/dbname?schema=public

# NextAuth / Email provider
# SMTP server URL (RFC-1738). Example MailTrap: smtp://USER:PASSWORD@smtp.mailtrap.io:2525
EMAIL_SERVER=smtp://user:password@smtp.example.com:587
# From address for magic link emails
EMAIL_FROM="Bookmy <no-reply@yourdomain.com>"
# Generate a secure random string for NEXTAUTH_SECRET (use `openssl rand -base64 32`)
NEXTAUTH_SECRET=your_nextauth_secret_here
