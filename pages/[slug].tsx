// This page has been deprecated.
// We moved from public pages under `/[slug]` to a subdomain-based approach (salon.bookmy.ro).
// Keep a simple message here to avoid broken imports; remove this file later if you prefer.

export default function DeprecatedSalonPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">Pagina salonului nu mai există</h1>
      <p className="mt-2">Am schimbat arhitectura — folosește subdomeniul de tipul <code>salonultau.bookmy.ro</code>.</p>
    </main>
  )
}
